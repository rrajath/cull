This file describes all the bugs in the application. Some of them are UI and some are functionality related.

- there's an arrow on the giant Library button on the home screen. That needs to be removed.
- vertical swipe gestures must be removed. Instead a delete button must be added in the HUD next to the Pin button. Tapping this delete button must mark the image for deletion and change the image to greyscale
- in the library grid view, the photo that is pinned must be indicated in the thumbnail with a pin
- in the library grid view, the photos that are marked for deletion must be indicated with a trash badge on the thumbnail and the thumbnail must be greyed out.
